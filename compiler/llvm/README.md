# LLVM Backend Module

LLVM IR emission, optimization pass pipeline, and native object file generation.

## Future Integration

```cmake
find_package(LLVM REQUIRED CONFIG)
target_link_libraries(myc_llvm PRIVATE LLVM)
```

## Status

Architecture placeholder — LLVM not yet linked.
